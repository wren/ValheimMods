## Settings file was created by plugin HammerTime v0.3.9
## Plugin GUID: com.maxsch.valheim.HammerTime

[1 - General]

## Disables crafting recipes of custom hammers that are enabled in this mod. Only deactivates the recipes, existing items will not be removed. Can be changed at runtime.
# Setting type: Boolean
# Default value: true
Disable Hammer Recipes = true

[Candles__Lanterns_and_Beeswax _HammerPieceTable]

## Enables moving pieces from this custom hammer into the vanilla hammer. Can be changed at runtime.
# Setting type: Boolean
# Default value: false
Enable Hammer = false

## Combines all categories from this custom hammer into one category. Can be changed at runtime.
# Setting type: Boolean
# Default value: true
Combine Categories = true

## Used category name if categories are combined. A new category is getting created if it is not a vanilla value. If two categories have the same name the pieces will be combined, even from different hammers. Can be changed at runtime.
# Setting type: String
# Default value: Candles__Lanterns_and_Beeswax
Combined Category Name = Candles__Lanterns_and_Beeswax

## Used category name if categories are not combined. A new category is getting created if it is not a vanilla value. If two categories have the same name the pieces will be combined, even from different hammers. Can be changed at runtime.
# Setting type: String
# Default value: Candles__Lanterns_and_Beeswax BuildingStonecutter
Category Name BuildingStonecutter = Candles__Lanterns_and_Beeswax BuildingStonecutter

[Clutter _ClutterPieceTable]

## Enables moving pieces from this custom hammer into the vanilla hammer. Can be changed at runtime.
# Setting type: Boolean
# Default value: true
Enable Hammer = true

## Combines all categories from this custom hammer into one category. Can be changed at runtime.
# Setting type: Boolean
# Default value: false
Combine Categories = false

## Used category name if categories are combined. A new category is getting created if it is not a vanilla value. If two categories have the same name the pieces will be combined, even from different hammers. Can be changed at runtime.
# Setting type: String
# Default value: Clutter
Combined Category Name = Clutter

## Used category name if categories are not combined. A new category is getting created if it is not a vanilla value. If two categories have the same name the pieces will be combined, even from different hammers. Can be changed at runtime.
# Setting type: String
# Default value: Clutter Decor
Category Name Decor = Clutter Decor

## Used category name if categories are not combined. A new category is getting created if it is not a vanilla value. If two categories have the same name the pieces will be combined, even from different hammers. Can be changed at runtime.
# Setting type: String
# Default value: Clutter Clutter Decor
Category Name Clutter Decor = Clutter Clutter Decor

## Used category name if categories are not combined. A new category is getting created if it is not a vanilla value. If two categories have the same name the pieces will be combined, even from different hammers. Can be changed at runtime.
# Setting type: String
# Default value: Clutter Clutter Amenities
Category Name Clutter Amenities = Clutter Clutter Amenities

## Used category name if categories are not combined. A new category is getting created if it is not a vanilla value. If two categories have the same name the pieces will be combined, even from different hammers. Can be changed at runtime.
# Setting type: String
# Default value: Clutter Amenities
Category Name Amenities = Clutter Amenities

## Used category name if categories are not combined. A new category is getting created if it is not a vanilla value. If two categories have the same name the pieces will be combined, even from different hammers. Can be changed at runtime.
# Setting type: String
# Default value: Clutter Kitchen
Category Name Kitchen = Clutter Kitchen

## Used category name if categories are not combined. A new category is getting created if it is not a vanilla value. If two categories have the same name the pieces will be combined, even from different hammers. Can be changed at runtime.
# Setting type: String
# Default value: Clutter Sculptures
Category Name Sculptures = Clutter Sculptures

[Clutter _HammerPieceTable]

## Enables moving pieces from this custom hammer into the vanilla hammer. Can be changed at runtime.
# Setting type: Boolean
# Default value: false
Enable Hammer = false

## Combines all categories from this custom hammer into one category. Can be changed at runtime.
# Setting type: Boolean
# Default value: false
Combine Categories = false

## Used category name if categories are combined. A new category is getting created if it is not a vanilla value. If two categories have the same name the pieces will be combined, even from different hammers. Can be changed at runtime.
# Setting type: String
# Default value: Clutter
Combined Category Name = Clutter

## Used category name if categories are not combined. A new category is getting created if it is not a vanilla value. If two categories have the same name the pieces will be combined, even from different hammers. Can be changed at runtime.
# Setting type: String
# Default value: Clutter Clutter Decor
Category Name Clutter Decor = Clutter Clutter Decor

[CoreWoodExtras _HammerPieceTable]

## Enables moving pieces from this custom hammer into the vanilla hammer. Can be changed at runtime.
# Setting type: Boolean
# Default value: false
Enable Hammer = false

## Combines all categories from this custom hammer into one category. Can be changed at runtime.
# Setting type: Boolean
# Default value: false
Combine Categories = false

## Used category name if categories are combined. A new category is getting created if it is not a vanilla value. If two categories have the same name the pieces will be combined, even from different hammers. Can be changed at runtime.
# Setting type: String
# Default value: CoreWoodExtras
Combined Category Name = CoreWoodExtras

## Used category name if categories are not combined. A new category is getting created if it is not a vanilla value. If two categories have the same name the pieces will be combined, even from different hammers. Can be changed at runtime.
# Setting type: String
# Default value: CoreWoodExtras CoreWood-Building
Category Name CoreWood-Building = CoreWoodExtras CoreWood-Building

## Used category name if categories are not combined. A new category is getting created if it is not a vanilla value. If two categories have the same name the pieces will be combined, even from different hammers. Can be changed at runtime.
# Setting type: String
# Default value: CoreWoodExtras CoreWood-Greenhouse
Category Name CoreWood-Greenhouse = CoreWoodExtras CoreWood-Greenhouse

## Used category name if categories are not combined. A new category is getting created if it is not a vanilla value. If two categories have the same name the pieces will be combined, even from different hammers. Can be changed at runtime.
# Setting type: String
# Default value: CoreWoodExtras CoreWood-Market
Category Name CoreWood-Market = CoreWoodExtras CoreWood-Market

## Used category name if categories are not combined. A new category is getting created if it is not a vanilla value. If two categories have the same name the pieces will be combined, even from different hammers. Can be changed at runtime.
# Setting type: String
# Default value: CoreWoodExtras CoreWood-Furniture
Category Name CoreWood-Furniture = CoreWoodExtras CoreWood-Furniture

## Used category name if categories are not combined. A new category is getting created if it is not a vanilla value. If two categories have the same name the pieces will be combined, even from different hammers. Can be changed at runtime.
# Setting type: String
# Default value: CoreWoodExtras CoreWood-Clutter
Category Name CoreWood-Clutter = CoreWoodExtras CoreWood-Clutter

[CraftyCarts _HammerPieceTable]

## Enables moving pieces from this custom hammer into the vanilla hammer. Can be changed at runtime.
# Setting type: Boolean
# Default value: false
Enable Hammer = false

## Combines all categories from this custom hammer into one category. Can be changed at runtime.
# Setting type: Boolean
# Default value: true
Combine Categories = true

## Used category name if categories are combined. A new category is getting created if it is not a vanilla value. If two categories have the same name the pieces will be combined, even from different hammers. Can be changed at runtime.
# Setting type: String
# Default value: CraftyCarts
Combined Category Name = CraftyCarts

## Used category name if categories are not combined. A new category is getting created if it is not a vanilla value. If two categories have the same name the pieces will be combined, even from different hammers. Can be changed at runtime.
# Setting type: String
# Default value: CraftyCarts Crafty Carts
Category Name Crafty Carts = CraftyCarts Crafty Carts

[MissingPieces _HammerPieceTable]

## Enables moving pieces from this custom hammer into the vanilla hammer. Can be changed at runtime.
# Setting type: Boolean
# Default value: false
Enable Hammer = false

## Combines all categories from this custom hammer into one category. Can be changed at runtime.
# Setting type: Boolean
# Default value: true
Combine Categories = true

## Used category name if categories are combined. A new category is getting created if it is not a vanilla value. If two categories have the same name the pieces will be combined, even from different hammers. Can be changed at runtime.
# Setting type: String
# Default value: MissingPieces
Combined Category Name = MissingPieces

## Used category name if categories are not combined. A new category is getting created if it is not a vanilla value. If two categories have the same name the pieces will be combined, even from different hammers. Can be changed at runtime.
# Setting type: String
# Default value: MissingPieces BuildingWorkbench
Category Name BuildingWorkbench = MissingPieces BuildingWorkbench

## Used category name if categories are not combined. A new category is getting created if it is not a vanilla value. If two categories have the same name the pieces will be combined, even from different hammers. Can be changed at runtime.
# Setting type: String
# Default value: MissingPieces BuildingStonecutter
Category Name BuildingStonecutter = MissingPieces BuildingStonecutter

## Used category name if categories are not combined. A new category is getting created if it is not a vanilla value. If two categories have the same name the pieces will be combined, even from different hammers. Can be changed at runtime.
# Setting type: String
# Default value: MissingPieces Furniture
Category Name Furniture = MissingPieces Furniture

[MoreGates++ _HammerPieceTable]

## Enables moving pieces from this custom hammer into the vanilla hammer. Can be changed at runtime.
# Setting type: Boolean
# Default value: false
Enable Hammer = false

## Combines all categories from this custom hammer into one category. Can be changed at runtime.
# Setting type: Boolean
# Default value: true
Combine Categories = true

## Used category name if categories are combined. A new category is getting created if it is not a vanilla value. If two categories have the same name the pieces will be combined, even from different hammers. Can be changed at runtime.
# Setting type: String
# Default value: MoreGates++
Combined Category Name = MoreGates++

## Used category name if categories are not combined. A new category is getting created if it is not a vanilla value. If two categories have the same name the pieces will be combined, even from different hammers. Can be changed at runtime.
# Setting type: String
# Default value: MoreGates++ Gates
Category Name Gates = MoreGates++ Gates

## Used category name if categories are not combined. A new category is getting created if it is not a vanilla value. If two categories have the same name the pieces will be combined, even from different hammers. Can be changed at runtime.
# Setting type: String
# Default value: MoreGates++ Misc
Category Name Misc = MoreGates++ Misc

## Used category name if categories are not combined. A new category is getting created if it is not a vanilla value. If two categories have the same name the pieces will be combined, even from different hammers. Can be changed at runtime.
# Setting type: String
# Default value: MoreGates++ BuildingWorkbench
Category Name BuildingWorkbench = MoreGates++ BuildingWorkbench

## Used category name if categories are not combined. A new category is getting created if it is not a vanilla value. If two categories have the same name the pieces will be combined, even from different hammers. Can be changed at runtime.
# Setting type: String
# Default value: MoreGates++ Furniture
Category Name Furniture = MoreGates++ Furniture

[PlanBuild _BlueprintPieceTable]

## Enables moving pieces from this custom hammer into the vanilla hammer. Can be changed at runtime.
# Setting type: Boolean
# Default value: false
Enable Hammer = false

## Combines all categories from this custom hammer into one category. Can be changed at runtime.
# Setting type: Boolean
# Default value: false
Combine Categories = false

## Used category name if categories are combined. A new category is getting created if it is not a vanilla value. If two categories have the same name the pieces will be combined, even from different hammers. Can be changed at runtime.
# Setting type: String
# Default value: PlanBuild
Combined Category Name = PlanBuild

## Used category name if categories are not combined. A new category is getting created if it is not a vanilla value. If two categories have the same name the pieces will be combined, even from different hammers. Can be changed at runtime.
# Setting type: String
# Default value: PlanBuild Tools
Category Name Tools = PlanBuild Tools

[PlanBuild _HammerPieceTable]

## Enables moving pieces from this custom hammer into the vanilla hammer. Can be changed at runtime.
# Setting type: Boolean
# Default value: false
Enable Hammer = false

## Combines all categories from this custom hammer into one category. Can be changed at runtime.
# Setting type: Boolean
# Default value: false
Combine Categories = false

## Used category name if categories are combined. A new category is getting created if it is not a vanilla value. If two categories have the same name the pieces will be combined, even from different hammers. Can be changed at runtime.
# Setting type: String
# Default value: PlanBuild
Combined Category Name = PlanBuild

## Used category name if categories are not combined. A new category is getting created if it is not a vanilla value. If two categories have the same name the pieces will be combined, even from different hammers. Can be changed at runtime.
# Setting type: String
# Default value: PlanBuild BuildingStonecutter
Category Name BuildingStonecutter = PlanBuild BuildingStonecutter

## Used category name if categories are not combined. A new category is getting created if it is not a vanilla value. If two categories have the same name the pieces will be combined, even from different hammers. Can be changed at runtime.
# Setting type: String
# Default value: PlanBuild Misc
Category Name Misc = PlanBuild Misc

[PlanBuild _PlanHammerPieceTable]

## Enables moving pieces from this custom hammer into the vanilla hammer. Can be changed at runtime.
# Setting type: Boolean
# Default value: false
Enable Hammer = false

## Combines all categories from this custom hammer into one category. Can be changed at runtime.
# Setting type: Boolean
# Default value: false
Combine Categories = false

## Used category name if categories are combined. A new category is getting created if it is not a vanilla value. If two categories have the same name the pieces will be combined, even from different hammers. Can be changed at runtime.
# Setting type: String
# Default value: PlanBuild
Combined Category Name = PlanBuild

## Used category name if categories are not combined. A new category is getting created if it is not a vanilla value. If two categories have the same name the pieces will be combined, even from different hammers. Can be changed at runtime.
# Setting type: String
# Default value: PlanBuild Decor
Category Name Decor = PlanBuild Decor

## Used category name if categories are not combined. A new category is getting created if it is not a vanilla value. If two categories have the same name the pieces will be combined, even from different hammers. Can be changed at runtime.
# Setting type: String
# Default value: PlanBuild Clutter Decor
Category Name Clutter Decor = PlanBuild Clutter Decor

## Used category name if categories are not combined. A new category is getting created if it is not a vanilla value. If two categories have the same name the pieces will be combined, even from different hammers. Can be changed at runtime.
# Setting type: String
# Default value: PlanBuild Clutter Amenities
Category Name Clutter Amenities = PlanBuild Clutter Amenities

## Used category name if categories are not combined. A new category is getting created if it is not a vanilla value. If two categories have the same name the pieces will be combined, even from different hammers. Can be changed at runtime.
# Setting type: String
# Default value: PlanBuild Amenities
Category Name Amenities = PlanBuild Amenities

## Used category name if categories are not combined. A new category is getting created if it is not a vanilla value. If two categories have the same name the pieces will be combined, even from different hammers. Can be changed at runtime.
# Setting type: String
# Default value: PlanBuild Kitchen
Category Name Kitchen = PlanBuild Kitchen

## Used category name if categories are not combined. A new category is getting created if it is not a vanilla value. If two categories have the same name the pieces will be combined, even from different hammers. Can be changed at runtime.
# Setting type: String
# Default value: PlanBuild Sculptures
Category Name Sculptures = PlanBuild Sculptures

## Used category name if categories are not combined. A new category is getting created if it is not a vanilla value. If two categories have the same name the pieces will be combined, even from different hammers. Can be changed at runtime.
# Setting type: String
# Default value: PlanBuild Misc
Category Name Misc = PlanBuild Misc

## Used category name if categories are not combined. A new category is getting created if it is not a vanilla value. If two categories have the same name the pieces will be combined, even from different hammers. Can be changed at runtime.
# Setting type: String
# Default value: PlanBuild BuildingStonecutter
Category Name BuildingStonecutter = PlanBuild BuildingStonecutter

## Used category name if categories are not combined. A new category is getting created if it is not a vanilla value. If two categories have the same name the pieces will be combined, even from different hammers. Can be changed at runtime.
# Setting type: String
# Default value: PlanBuild BuildingWorkbench
Category Name BuildingWorkbench = PlanBuild BuildingWorkbench

## Used category name if categories are not combined. A new category is getting created if it is not a vanilla value. If two categories have the same name the pieces will be combined, even from different hammers. Can be changed at runtime.
# Setting type: String
# Default value: PlanBuild Crafting
Category Name Crafting = PlanBuild Crafting

## Used category name if categories are not combined. A new category is getting created if it is not a vanilla value. If two categories have the same name the pieces will be combined, even from different hammers. Can be changed at runtime.
# Setting type: String
# Default value: PlanBuild VALKEA Furniture
Category Name VALKEA Furniture = PlanBuild VALKEA Furniture

## Used category name if categories are not combined. A new category is getting created if it is not a vanilla value. If two categories have the same name the pieces will be combined, even from different hammers. Can be changed at runtime.
# Setting type: String
# Default value: PlanBuild VALKEA Building
Category Name VALKEA Building = PlanBuild VALKEA Building

## Used category name if categories are not combined. A new category is getting created if it is not a vanilla value. If two categories have the same name the pieces will be combined, even from different hammers. Can be changed at runtime.
# Setting type: String
# Default value: PlanBuild Furniture
Category Name Furniture = PlanBuild Furniture

## Used category name if categories are not combined. A new category is getting created if it is not a vanilla value. If two categories have the same name the pieces will be combined, even from different hammers. Can be changed at runtime.
# Setting type: String
# Default value: PlanBuild Crafty Carts
Category Name Crafty Carts = PlanBuild Crafty Carts

## Used category name if categories are not combined. A new category is getting created if it is not a vanilla value. If two categories have the same name the pieces will be combined, even from different hammers. Can be changed at runtime.
# Setting type: String
# Default value: PlanBuild Gates
Category Name Gates = PlanBuild Gates

## Used category name if categories are not combined. A new category is getting created if it is not a vanilla value. If two categories have the same name the pieces will be combined, even from different hammers. Can be changed at runtime.
# Setting type: String
# Default value: PlanBuild CoreWood-Building
Category Name CoreWood-Building = PlanBuild CoreWood-Building

## Used category name if categories are not combined. A new category is getting created if it is not a vanilla value. If two categories have the same name the pieces will be combined, even from different hammers. Can be changed at runtime.
# Setting type: String
# Default value: PlanBuild CoreWood-Greenhouse
Category Name CoreWood-Greenhouse = PlanBuild CoreWood-Greenhouse

## Used category name if categories are not combined. A new category is getting created if it is not a vanilla value. If two categories have the same name the pieces will be combined, even from different hammers. Can be changed at runtime.
# Setting type: String
# Default value: PlanBuild CoreWood-Market
Category Name CoreWood-Market = PlanBuild CoreWood-Market

## Used category name if categories are not combined. A new category is getting created if it is not a vanilla value. If two categories have the same name the pieces will be combined, even from different hammers. Can be changed at runtime.
# Setting type: String
# Default value: PlanBuild CoreWood-Furniture
Category Name CoreWood-Furniture = PlanBuild CoreWood-Furniture

## Used category name if categories are not combined. A new category is getting created if it is not a vanilla value. If two categories have the same name the pieces will be combined, even from different hammers. Can be changed at runtime.
# Setting type: String
# Default value: PlanBuild CoreWood-Clutter
Category Name CoreWood-Clutter = PlanBuild CoreWood-Clutter

## Used category name if categories are not combined. A new category is getting created if it is not a vanilla value. If two categories have the same name the pieces will be combined, even from different hammers. Can be changed at runtime.
# Setting type: String
# Default value: PlanBuild Raft
Category Name Raft = PlanBuild Raft

Category Name VALKEA BuildingStonecutter = PlanBuild VALKEA BuildingStonecutter

[ValheimCuisine _HammerPieceTable]

## Enables moving pieces from this custom hammer into the vanilla hammer. Can be changed at runtime.
# Setting type: Boolean
# Default value: false
Enable Hammer = false

## Combines all categories from this custom hammer into one category. Can be changed at runtime.
# Setting type: Boolean
# Default value: true
Combine Categories = true

## Used category name if categories are combined. A new category is getting created if it is not a vanilla value. If two categories have the same name the pieces will be combined, even from different hammers. Can be changed at runtime.
# Setting type: String
# Default value: ValheimCuisine
Combined Category Name = ValheimCuisine

## Used category name if categories are not combined. A new category is getting created if it is not a vanilla value. If two categories have the same name the pieces will be combined, even from different hammers. Can be changed at runtime.
# Setting type: String
# Default value: ValheimCuisine Crafting
Category Name Crafting = ValheimCuisine Crafting

[ValheimRAFT _HammerPieceTable]

## Enables moving pieces from this custom hammer into the vanilla hammer. Can be changed at runtime.
# Setting type: Boolean
# Default value: false
Enable Hammer = false

## Combines all categories from this custom hammer into one category. Can be changed at runtime.
# Setting type: Boolean
# Default value: true
Combine Categories = true

## Used category name if categories are combined. A new category is getting created if it is not a vanilla value. If two categories have the same name the pieces will be combined, even from different hammers. Can be changed at runtime.
# Setting type: String
# Default value: ValheimRAFT
Combined Category Name = ValheimRAFT

## Used category name if categories are not combined. A new category is getting created if it is not a vanilla value. If two categories have the same name the pieces will be combined, even from different hammers. Can be changed at runtime.
# Setting type: String
# Default value: ValheimRAFT Raft
Category Name Raft = ValheimRAFT Raft

[VALKEA _BJORKSNASPieceTable]

## Enables moving pieces from this custom hammer into the vanilla hammer. Can be changed at runtime.
# Setting type: Boolean
# Default value: true
Enable Hammer = true

## Combines all categories from this custom hammer into one category. Can be changed at runtime.
# Setting type: Boolean
# Default value: false
Combine Categories = false

## Used category name if categories are combined. A new category is getting created if it is not a vanilla value. If two categories have the same name the pieces will be combined, even from different hammers. Can be changed at runtime.
# Setting type: String
# Default value: VALKEA
Combined Category Name = VALKEA

## Used category name if categories are not combined. A new category is getting created if it is not a vanilla value. If two categories have the same name the pieces will be combined, even from different hammers. Can be changed at runtime.
# Setting type: String
# Default value: VALKEA Misc
Category Name Misc = VALKEA Food

## Used category name if categories are not combined. A new category is getting created if it is not a vanilla value. If two categories have the same name the pieces will be combined, even from different hammers. Can be changed at runtime.
# Setting type: String
# Default value: VALKEA BuildingStonecutter
Category Name BuildingStonecutter = VALKEA Furniture

## Used category name if categories are not combined. A new category is getting created if it is not a vanilla value. If two categories have the same name the pieces will be combined, even from different hammers. Can be changed at runtime.
# Setting type: String
# Default value: VALKEA BuildingWorkbench
Category Name BuildingWorkbench = VALKEA Building

## Used category name if categories are not combined. A new category is getting created if it is not a vanilla value. If two categories have the same name the pieces will be combined, even from different hammers. Can be changed at runtime.
# Setting type: String
# Default value: VALKEA Crafting
Category Name Crafting = VALKEA Accessories

## Used category name if categories are not combined. A new category is getting created if it is not a vanilla value. If two categories have the same name the pieces will be combined, even from different hammers. Can be changed at runtime.
# Setting type: String
# Default value: VALKEA VALKEA Furniture
Category Name VALKEA Furniture = VALKEA VALKEA Furniture

## Used category name if categories are not combined. A new category is getting created if it is not a vanilla value. If two categories have the same name the pieces will be combined, even from different hammers. Can be changed at runtime.
# Setting type: String
# Default value: VALKEA VALKEA Building
Category Name VALKEA Building = VALKEA VALKEA Building

Category Name VALKEA BuildingStonecutter = VALKEA VALKEA BuildingStonecutter

[VALKEA _HammerPieceTable]

## Enables moving pieces from this custom hammer into the vanilla hammer. Can be changed at runtime.
# Setting type: Boolean
# Default value: false
Enable Hammer = false

## Combines all categories from this custom hammer into one category. Can be changed at runtime.
# Setting type: Boolean
# Default value: false
Combine Categories = false

## Used category name if categories are combined. A new category is getting created if it is not a vanilla value. If two categories have the same name the pieces will be combined, even from different hammers. Can be changed at runtime.
# Setting type: String
# Default value: VALKEA
Combined Category Name = VALKEA

## Used category name if categories are not combined. A new category is getting created if it is not a vanilla value. If two categories have the same name the pieces will be combined, even from different hammers. Can be changed at runtime.
# Setting type: String
# Default value: VALKEA VALKEA Building
Category Name VALKEA Building = VALKEA VALKEA Building

## Used category name if categories are not combined. A new category is getting created if it is not a vanilla value. If two categories have the same name the pieces will be combined, even from different hammers. Can be changed at runtime.
# Setting type: String
# Default value: VALKEA VALKEA Furniture
Category Name VALKEA Furniture = VALKEA VALKEA Furniture

Category Name VALKEA BuildingStonecutter = VALKEA VALKEA BuildingStonecutter

[Vanilla _HammerPieceTable]

## Enables moving pieces from this custom hammer into the vanilla hammer. Can be changed at runtime.
# Setting type: Boolean
# Default value: false
Enable Hammer = false

## Combines all categories from this custom hammer into one category. Can be changed at runtime.
# Setting type: Boolean
# Default value: false
Combine Categories = false

## Used category name if categories are combined. A new category is getting created if it is not a vanilla value. If two categories have the same name the pieces will be combined, even from different hammers. Can be changed at runtime.
# Setting type: String
# Default value: Vanilla
Combined Category Name = Vanilla

## Used category name if categories are not combined. A new category is getting created if it is not a vanilla value. If two categories have the same name the pieces will be combined, even from different hammers. Can be changed at runtime.
# Setting type: String
# Default value: Misc
Category Name Misc = Misc

## Used category name if categories are not combined. A new category is getting created if it is not a vanilla value. If two categories have the same name the pieces will be combined, even from different hammers. Can be changed at runtime.
# Setting type: String
# Default value: Crafting
Category Name Crafting = Crafting

## Used category name if categories are not combined. A new category is getting created if it is not a vanilla value. If two categories have the same name the pieces will be combined, even from different hammers. Can be changed at runtime.
# Setting type: String
# Default value: BuildingWorkbench
Category Name BuildingWorkbench = BuildingWorkbench

## Used category name if categories are not combined. A new category is getting created if it is not a vanilla value. If two categories have the same name the pieces will be combined, even from different hammers. Can be changed at runtime.
# Setting type: String
# Default value: BuildingStonecutter
Category Name BuildingStonecutter = BuildingStonecutter

## Used category name if categories are not combined. A new category is getting created if it is not a vanilla value. If two categories have the same name the pieces will be combined, even from different hammers. Can be changed at runtime.
# Setting type: String
# Default value: Furniture
Category Name Furniture = Furniture

